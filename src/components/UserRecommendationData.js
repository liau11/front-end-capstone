

const UserRecommendationData = ({ name, address }) => {


    return (
        <section>
            <h3>{name}</h3>
            <div>{address}</div>
        </section>
    )
};

export default UserRecommendationData;

